#!/bin/bash

# Get the path that this script is located
SCRIPTLOC=`dirname $0`
# Move to the directory of this script so
# that path relative configuration works
cd $SCRIPTLOC

# get user input
${SCRIPTLOC}/adminInterface

# regenerate website
${SCRIPTLOC}/generateWebsite
